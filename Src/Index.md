# Inicio rápido

1. Copia `.env.example` a `.env` y configura las variables.
2. Ejecuta `pip install -r requirements.txt` dentro de un entorno virtual.
3. Lanza el servidor con `flask --app manage.py run`.

Visita `http://127.0.0.1:5000` para usar la plataforma.
